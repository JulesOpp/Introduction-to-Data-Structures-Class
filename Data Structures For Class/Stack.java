public class Stack
{
    LinkedList list;
    
    // Instantiate and initialize the linked list
    public Stack() { 
        list = new LinkedList(); 
    }
    
    // Add a new head (cast the character as an integer)
    public void push(char ch) {
        
    }
    
    // Remove and return the top of the list
    public char pop() {

    }
    
    // Return the top of the list without removing it (use the getNode method)
    public char top() {

    }
    
    // Check whether the list is empty
    public boolean isEmpty() {

    }
}